[providers]
  node = true

[phases.setup]
  nixPkgs = ["ffmpeg"]

[phases.build]
  cmds = [
    "npm run build",
    "curl -L https://github.com/yt-dlp/yt-dlp-nightly-builds/releases/latest/download/yt-dlp -o dist/yt-dlp",
    "chmod +x dist/yt-dlp"
  ]
